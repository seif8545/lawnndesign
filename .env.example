# Frontend environment variables (Vite)
#
# Copy this to `.env.local` for local dev. For the Cloudflare Pages
# deployment, set VITE_API_URL in:
#   Pages dashboard → your project → Settings → Environment variables → Production
#
# Must point at the backend server (NOT the frontend origin).

VITE_API_URL="http://localhost:3001"
